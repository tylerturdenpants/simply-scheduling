<div class="card mb-3">
	<div class="card-body d-flex justify-content-between align-items-center">
		<div>
			<div class="h6">{{this.providedService}}</div>
			<p class="m-0">{{this.duration}}</p>
			{{yield}}
		</div>
		<button class="btn btn-primary ml-3">Select</button>
	</div>
</div>

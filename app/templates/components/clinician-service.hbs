<div class="card mb-3">
	<div class="card-body d-flex justify-content-between align-items-center">
		<div>
			<div class="h6">{{@srvc.description}}</div>
			<p class="m-0">{{@srvc.duration}} minutes</p>
			{{yield}}
		</div>
		<button {{on 'click' (action this.goToLocations @srvc.id) }} class="btn btn-primary ml-3" data-service-id="{{@srvc.id}}">Select</button>
	</div>
</div>
